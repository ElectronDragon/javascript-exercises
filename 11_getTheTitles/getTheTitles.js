const getTheTitles = function(books) {
    let titleList = books.map(i => i.title);

    return titleList;

};

//  I needed a lot of help with this one. I don't think I understand .map as well as I need to, but it's slowly coming together.

// Do not edit below this line
module.exports = getTheTitles;
